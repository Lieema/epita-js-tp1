// FIXME: add some meaningful data

const pictures = [];

const firstId = 155;
for (let index = 0; index < 5; index++) {
    pictures[index] = "https://picsum.photos/" + firstId.toString() + "/354";    
}

export default pictures;
